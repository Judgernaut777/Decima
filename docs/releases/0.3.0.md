# Decima 0.3.0 — Local Daily Driver

> **EXPERIMENTAL — reference / prototype.** Decima 0.3 is not a finished product. Run it only on
> data you can afford to lose. It is a single-user, loopback-only local daemon; it is not a
> production, multi-user, or remotely-exposed service.

> **BACKUP FIRST.** The append-only Weft is your only canonical state. Take a backup with
> `decima-backup` before upgrading, before any experiment, and on a regular cadence. Backups
> exclude your signing keys by design — keep those keys backed up separately and privately.

Package version: `0.3.0` (bumped from the `0.3.0.dev0` candidate at the tag build). The `v0.3.0`
tag points at the fully-gated release commit — see [`../RELEASE-READINESS.md`](../RELEASE-READINESS.md). The
independent audit verdict is **APPROVE WITH DOCUMENTED LIMITATIONS**
(`../release-evidence/audit/0.3-audit.md`).

## What Decima 0.3 is

Decima 0.3 turns the frozen `heartbeat/` reference implementation into a locally-hosted
daily-driver app. It ships a clean `decima/` package **extracted from and proven equivalent to**
`heartbeat/` (byte-equal via golden conformance fixtures), fronted by a **trusted Shell**: one
program you log into on `127.0.0.1` that connects you to your notes, tasks, projects, agents, and
approvals — plus three daily-driver workflows (grounded Q&A, model-planned durable agents, and an
isolated coding workspace) — with the system holding the authority so a rogue or prompt-injected
agent still can't act outside its capabilities.

## What is usable now

Three daily-driver workflows are delivered **through the rendered Shell** — each driven
end-to-end through visible controls and qualified by a Playwright spec against the real backend:

- **Grounded Q&A** — import documents, choose a retrieval scope, ask a question, and get a
  generated answer with **citations that open the real source passage** they cite. Generated text
  is visually distinct from imported source data, and hostile imports stay inert DATA
  (`qa.spec.js`; `decima/services/api/qa_service.py`; `js/screens/qa.js`).
- **Model-planned durable agents** — type an objective, a routed **model proposes** a plan,
  deterministic code validates it, and only an explicit **AcceptPlanProposal** mints durable
  Plan / Step / Agent Cells that the scheduler executes; **pause / resume / cancel / gated
  terminate** are server-enforced, and proposed vs. authorized vs. executed are visually distinct
  (`planning.spec.js`; `plan_service.py`; `js/screens/plans.js`).
- **Isolated coding workspace** — grant a repository root, request a bounded change, and it runs
  ONLY inside a **jailed, networkless `decima.workers` child** (no push, no credential, no
  network); durable diff + test artifacts render as untrusted text, the source repo outside is
  untouched, and a restart recovers the run honestly (`workspace.spec.js`; `workspace_service.py`;
  `js/screens/workspace.js`).

Alongside them, the trusted core surfaces:

- **Notes** in trust zones, each with durable **provenance** (the Weft event ids that asserted it)
- **Tasks** and **projects**
- The **agent inspector** — objective, principal, budgets, deadline, status
- The trusted **approval inbox** — gated terminate / revoke proposals defer to a reauth-gated approve
- **Capabilities**, **activity**, and per-item provenance

Operationally: clean install + first-run, `decima-doctor` (exit 0), `decima-backup` /
`decima-restore` / `decima-rebuild` (byte-identical `state_root` round-trip verified, every new
cell kind survives), and a systemd user service. The CLI ops honor process arguments when invoked
as installed console scripts.

## Architecture preserved

- **Four verbs over one log** — ASSERT · RETRACT · INVOKE · ATTEST are the entire durable
  instruction set; there is no special grant/revoke.
- **The Weft is the sole truth** — every mutation routes through the kernel; projections (the
  Weave, boards, knowledge, activity) are disposable and rebuildable, never canonical. The only
  non-kernel database access is a read-only `SELECT` in the backup service.
- **No ambient authority** — a principal can do exactly what its capabilities permit; models
  *propose* and deterministic code *authorizes* + gates every effect. The kernel import-boundary
  guard enforces this at test time.

## Install prerequisites

- Linux (qualified on **aarch64 / arm64**), **Python ≥ 3.11**
- One third-party dependency: **PyNaCl** (real Ed25519 via libsodium); everything else is stdlib
- Optional service lifecycle: **systemd** (a user instance) and, to reproduce the container lane,
  **Docker**

Run the Shell directly:

```bash
python3 -m decima.shell.serve <weft.db>   # http://127.0.0.1:8973/
```

The pairing secret is written to a `0600` `.pairing-secret` file beside the Weft (only its path is
printed; pass `--print-secret` to echo the value on an interactive first run).

## Environment actually tested

- Host: **AArch64 (arm64) Linux 6.6.10**, **Python 3.11.2**, PyNaCl 1.6.2
- Gate: **626 passed, 25 skipped** (`python3 -m pytest -q`); ruff `check` clean
- Browser: **13 Playwright specs across 9 files** in headless Chromium (bundled
  `chromium_headless_shell-1228`, arm64, `--no-sandbox`) against the real backend + Shell —
  including `qa`, `planning`, `workspace`, and `visual_a11y`
- Adversarial suite: **49 passed** (worker-isolation / containment included)
- Live inference: a real local **llama.cpp Qwen3-30B-A3B** on `127.0.0.1:8080`, selected by
  product routing, passes the app-path live suite (**9 passed / 1 skipped**) — no cloud credential
- Clean install + service / reboot lifecycle: proven in a **systemd-enabled Docker container**
  (23/23 [ok], exit 0) — the host systemd is degraded
- Frozen invariants: `heartbeat/` byte-identical vs the frozen heartbeat baseline (tree `30bb20dc`, carried by reachable `29bfe9a`); `decima/kernel/` + `protocol/`
  byte-identical vs `29bfe9a`

## Model-provider status

The **deterministic (offline) provider is the default** and the unconfigured path. A live provider
is **opt-in** via `DECIMA_LIVE_*` (names in `docs/operations/model-configuration.md`; no values in
the repo). The non-live qualification (routing + reason codes, budget block, privacy,
failure/fallback, **secret redaction — 0 leaks**) passes. The live path is **qualified against a
real local model**: a llama.cpp **Qwen3-30B-A3B** served OpenAI-compatible on `127.0.0.1:8080` is
**selected by the product's own routing** and round-trips grounded-question + plan proposals
through the normal abstraction (citations deterministically validated, malformed replies bounded,
budget enforced) — `tests/live/test_app_path_live.py` → 9 passed / 1 skipped
(`docs/release-evidence/models/shell-driven-live-routing.md`). **No cloud credential is used or
needed**; when `DECIMA_LIVE_*` is unset the deterministic provider remains the default.

## Security boundaries

- **Strict same-origin CSP** — `script-src 'self'`, no `unsafe-inline`/`unsafe-eval`,
  `object-src 'none'`, `base-uri 'none'`; plus nosniff and frame-deny. Imported HTML/MD is proven
  non-executing; the frontend ships a dedicated `sanitize.js`.
- **Unauthenticated `/api/*` → 401**; every write route is `WRITE`/`REAUTH`.
- **Approval chrome** — hostile Approve buttons / banners / inline-JS are inert; only the real
  reauth-gated component can approve (re-present the pairing secret per call).
- **Worker isolation** — capability execution runs in an isolated child (chroot jail, no network,
  no credentials); worker-escape suite green.
- **Backups exclude signing keys**; the doctor report/export are scrubbed (no secret leaks).

## Known limitations & deferred features

- **Single-user, loopback-only daemon** — `127.0.0.1` only; no multi-user, no remote exposure, no
  authentication beyond the local pairing secret.
- **Single-threaded Shell server** — serves single-threaded to keep all Weft access on the one
  `sqlite3` thread; `/stream` returns a finite snapshot so requests serialize but never deadlock.
  The kernel fix (`weft.py` `check_same_thread=False` + lock) is filed for **0.3.1**.
- **Deterministic provider is the default**; the live provider is opt-in via `DECIMA_LIVE_*` and
  qualified against a **local** model (no cloud credential).
- **Retrieval is local lexical scoring**, not embeddings / a vector index.
- **Service / reboot lifecycle** is proven in a systemd-enabled container (host systemd degraded),
  not on a bare host.
- **Intentionally out of 0.3** (handoff §3.2): financial automation, live brokerage, full browser
  automation, mobile, replication/multi-device sync, and the eventual single Rust port.

## Upgrade expectations

This is the first coherent 0.3 release; there is no prior `decima/`-package release to migrate
from. The Weft is append-only and projections rebuild from it, so a projection schema bump is a
clean rebuild, not a data migration. **Always take a `decima-backup` before upgrading.** The kernel
and protocol are frozen for 0.3; 0.3.1 will pick up the single-threaded-server kernel fix and
Stage-2 kernel cleanup.

## Release artifacts

Built on this host with the `setuptools.build_meta` backend (`python3 -m build` unavailable →
wheel via `pip wheel --no-deps --no-build-isolation .`, sdist via `setuptools.build_meta.build_sdist`).
Artifacts are **not** committed to the tree. The wheel was installed into a throwaway `--target`
**outside** the repo and imported from an unrelated cwd: `decima.shell.serve.FRONTEND_DIR` resolved
**under the target**, all **18** frontend files shipped — including the new `js/screens/qa.js`,
`js/screens/workspace.js`, and `js/screens/plans.js` (each served 200 and registered in the app
bundle) — and `GET /` resolves to `index.html` (serves 200).

Artifact **SHA-256 digests are recorded per-build in the qualification evidence**
(`docs/release-evidence/qualification/0.3-full-gate.md`, Gate 8) and the `0.3.0` tag-build digests
are recorded in `docs/release-evidence/qualification/0.3.0-artifacts.md`.

| Artifact | Install-from-artifact verified |
|---|---|
| `decima-<version>-py3-none-any.whl` | **yes** (isolated `--target`; `FRONTEND_DIR` under target; 18 assets incl. the three new screens; `GET /` → 200) |
| `decima-<version>.tar.gz` (sdist) | built (sdist; not install-verified) |

## Licensing note (pre-publish, operator decision)

`pyproject.toml` currently declares `license = "Proprietary"` and the `Private :: Do Not Upload`
classifier. This is **not** release-blocking for an unpublished candidate, but reconcile the
license before any public publish — it is an operator decision, not part of tagging.
